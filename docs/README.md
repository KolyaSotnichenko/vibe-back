# Vibe Backend API Documentation

Вітаємо в документації Vibe Backend API!
Welcome to the Vibe Backend API documentation!

## 📚 Доступна документація / Available Documentation

### [OPENAPI_SETUP.md](./OPENAPI_SETUP.md)
**Швидкий старт та встановлення**
**Quick start and installation guide**

- Інструкції зі встановлення / Installation instructions
- Як запустити сервер / How to start the server
- Базове використання / Basic usage
- Приклади для різних фронтенд фреймворків / Examples for different frontend frameworks

### [OPENAPI.md](./OPENAPI.md)
**Повна документація OpenAPI**
**Complete OpenAPI documentation**

- Детальний опис OpenAPI специфікації / Detailed OpenAPI specification description
- Інтеграція з фронтендом / Frontend integration
- Генерація клієнтів / Client generation
- Приклади використання / Usage examples
- CI/CD інтеграція / CI/CD integration

### openapi.json
**OpenAPI специфікація (генерується автоматично)**
**OpenAPI specification (auto-generated)**

Цей файл генерується командою:
This file is generated by running:

```bash
pnpm run openapi:generate
```

Ви можете використовувати його для:
You can use it to:
- Генерації TypeScript клієнтів / Generate TypeScript clients
- Імпорту в Postman/Insomnia / Import into Postman/Insomnia
- Автоматичної генерації UI / Automatic UI generation
- Валідації запитів / Request validation

## 🚀 Швидкий старт / Quick Start

### 1. Встановити залежності / Install dependencies
```bash
pnpm add @nestjs/swagger swagger-ui-express
```

### 2. Запустити сервер / Start the server
```bash
pnpm run start:dev
```

### 3. Відкрити документацію / Open documentation
- **Swagger UI**: http://localhost:3000/api
- **OpenAPI JSON**: http://localhost:3000/api-json/openapi.json
- **OpenAPI YAML**: http://localhost:3000/api-json/openapi.yaml

## 🔑 Аутентифікація / Authentication

Більшість endpoints вимагають API key в заголовку:
Most endpoints require an API key in the header:

```bash
X-API-Key: vibe-test-api-key-2024-secure-token-12345
```

## 📝 Основні endpoints / Main Endpoints

### Публічні / Public
- `GET /` - Вітальне повідомлення / Welcome message
- `GET /api` - Swagger UI
- `GET /api-json/openapi.json` - OpenAPI специфікація / OpenAPI specification

### Проекти / Projects (потрібен API key / requires API key)
- `GET /projects` - Отримати всі проекти / Get all projects
- `GET /projects/:id` - Отримати проект / Get project by ID
- `POST /projects` - Створити проект / Create project
- `PATCH /projects/:id` - Оновити проект / Update project
- `DELETE /projects/:id` - Видалити проект / Delete project
- `GET /projects/stats/count` - Кількість проектів / Project count

## 🛠️ Корисні команди / Useful Commands

```bash
# Запустити в режимі розробки / Start in development mode
pnpm run start:dev

# Згенерувати OpenAPI JSON / Generate OpenAPI JSON
pnpm run openapi:generate

# Збілдити проект / Build project
pnpm run build

# Запустити тести / Run tests
pnpm run test

# Запустити лінтер / Run linter
pnpm run lint
```

## 📊 Структура OpenAPI / OpenAPI Structure

Згенерована специфікація містить:
The generated specification includes:

- ✅ Всі endpoints з повними описами / All endpoints with full descriptions
- ✅ Схеми даних (DTOs, entities) / Data schemas (DTOs, entities)
- ✅ Валідаційні правила / Validation rules
- ✅ Приклади запитів та відповідей / Request and response examples
- ✅ Коди статусів / Status codes
- ✅ Схеми аутентифікації / Authentication schemes
- ✅ Теги для групування / Tags for grouping

## 🎯 Приклади використання / Usage Examples

### Fetch API
```typescript
const response = await fetch('http://localhost:3000/projects', {
  headers: {
    'X-API-Key': 'vibe-test-api-key-2024-secure-token-12345'
  }
});
const projects = await response.json();
```

### TypeScript Client (auto-generated)
```typescript
import { api } from './generated/client';

const { data, error } = await api.GET('/projects');
if (data) {
  console.log('Projects:', data);
}
```

### curl
```bash
curl http://localhost:3000/projects \
  -H "X-API-Key: vibe-test-api-key-2024-secure-token-12345"
```

## 🔗 Додаткові ресурси / Additional Resources

- [NestJS Documentation](https://docs.nestjs.com/)
- [OpenAPI Specification](https://swagger.io/specification/)
- [Swagger UI](https://swagger.io/tools/swagger-ui/)
- [OpenAPI Generator](https://openapi-generator.tech/)

## 📞 Підтримка / Support

Для питань та проблем перегляньте:
For questions and issues, review:

1. **Swagger UI**: http://localhost:3000/api (інтерактивна документація / interactive docs)
2. **OPENAPI_SETUP.md** - інструкції зі встановлення / setup instructions
3. **OPENAPI.md** - повна документація / complete documentation
4. **openapi.json** - згенерована специфікація / generated specification

---

**Останнє оновлення / Last updated**: February 2024
**Версія API / API Version**: 1.0
