# Quick Start Guide 🚀

## Що було створено / What Was Created

### ✅ Project Entity (Сутність Project)
Повноцінна сутність проекту з усіма полями:
- ID, name, description
- Status (PLANNING, IN_PROGRESS, ON_HOLD, COMPLETED, CANCELLED)
- Dates (startDate, endDate)
- Owner, tags
- Timestamps (createdAt, updatedAt)

### ✅ API Endpoints (Ендпоінти)
Усі необхідні CRUD операції:
- **POST /projects** - створити проект
- **GET /projects** - отримати всі проекти (з фільтрами)
- **GET /projects/:id** - отримати проект за ID
- **PATCH /projects/:id** - оновити проект
- **DELETE /projects/:id** - видалити проект
- **GET /projects/stats/count** - кількість проектів

### ✅ Security (Захист)
- API key authentication через X-API-Key header
- Тестовий ключ: **`vibe-test-api-key-2024-secure-token-12345`**
- Валідація всіх вхідних даних
- Захист від SQL injection (in-memory storage)

### ✅ Swagger Documentation
- Повна OpenAPI документація
- Детальні описи всіх ендпоінтів
- Приклади запитів та відповідей
- Інтерактивна UI для тестування

### ✅ In-Memory Storage
- Використовується Map для зберігання
- Немає залежностей від бази даних
- Ідеально для тестування

---

## Як запустити / How to Start

### Крок 1: Встановити залежності
```bash
npm install @nestjs/swagger swagger-ui-express class-validator class-transformer
```

Або використати готовий скрипт:
```bash
chmod +x install-deps.sh
./install-deps.sh
```

### Крок 2: Запустити додаток
```bash
npm run start:dev
```

### Крок 3: Відкрити Swagger UI
Браузер: **http://localhost:3000/api**

---

## Як протестувати / How to Test

### Варіант 1: Swagger UI (найпростіше)
1. Відкрити http://localhost:3000/api
2. Натиснути кнопку **"Authorize"** 🔓
3. Ввести ключ: `vibe-test-api-key-2024-secure-token-12345`
4. Натиснути **"Authorize"** → **"Close"**
5. Тестувати ендпоінти прямо в браузері! 🎉

### Варіант 2: Автоматичний тест скрипт
```bash
chmod +x test-api.sh
./test-api.sh
```

### Варіант 3: Postman
1. Імпортувати `postman-collection.json`
2. API key вже налаштований
3. Тестувати!

### Варіант 4: curl (приклад)
```bash
# Створити проект
curl -X POST http://localhost:3000/projects \
  -H "Content-Type: application/json" \
  -H "X-API-Key: vibe-test-api-key-2024-secure-token-12345" \
  -d '{
    "name": "Новий проект",
    "description": "Опис проекту",
    "status": "PLANNING",
    "startDate": "2024-03-01T00:00:00.000Z",
    "owner": "Іван Іванов",
    "tags": ["web", "frontend"]
  }'

# Отримати всі проекти
curl http://localhost:3000/projects \
  -H "X-API-Key: vibe-test-api-key-2024-secure-token-12345"
```

---

## Структура файлів / File Structure

```
✅ src/common/
   ├── decorators/public.decorator.ts     # @Public() decorator
   └── guards/api-key.guard.ts            # API key guard

✅ src/projects/
   ├── dto/
   │   ├── create-project.dto.ts          # DTO для створення
   │   └── update-project.dto.ts          # DTO для оновлення
   ├── entities/
   │   └── project.entity.ts              # Сутність Project
   ├── projects.controller.ts             # REST контролер
   ├── projects.service.ts                # Бізнес-логіка
   └── projects.module.ts                 # NestJS модуль

✅ Документація:
   ├── README_PROJECTS.md                 # Повна документація
   ├── SETUP.md                           # Інструкції по встановленню
   ├── QUICKSTART.md                      # Швидкий старт (цей файл)
   ├── postman-collection.json            # Postman колекція
   ├── test-api.sh                        # Тест скрипт
   └── install-deps.sh                    # Скрипт встановлення
```

---

## Приклади використання / Usage Examples

### Створити проект з тегами
```json
POST /projects
{
  "name": "Mobile App",
  "description": "iOS та Android додаток",
  "status": "PLANNING",
  "startDate": "2024-03-01T00:00:00.000Z",
  "endDate": "2024-12-31T00:00:00.000Z",
  "owner": "Product Team",
  "tags": ["mobile", "ios", "android"]
}
```

### Фільтрувати за статусом
```bash
GET /projects?status=IN_PROGRESS
```

### Фільтрувати за тегом
```bash
GET /projects?tag=mobile
```

### Оновити статус проекту
```json
PATCH /projects/{id}
{
  "status": "IN_PROGRESS"
}
```

---

## API Key для тестування / Test API Key

```
vibe-test-api-key-2024-secure-token-12345
```

Використовувати в header: `X-API-Key: vibe-test-api-key-2024-secure-token-12345`

---

## Статуси проектів / Project Statuses

- **PLANNING** - Планування
- **IN_PROGRESS** - В процесі
- **ON_HOLD** - Призупинено
- **COMPLETED** - Завершено
- **CANCELLED** - Скасовано

---

## Критерії прийняття ✅ / Acceptance Criteria

- ✅ **Сутність створена** - Project entity з усіма полями
- ✅ **Ендпоінти працюють** - Всі CRUD операції
- ✅ **Ендпоінти захищені** - API key authentication
- ✅ **Swagger документація** - Повна OpenAPI специфікація
- ✅ **Зберігання в пам'яті** - In-memory Map storage
- ✅ **Валідація** - class-validator для всіх DTO
- ✅ **Тестовий API ключ** - Готовий до використання

---

## Відомі обмеження / Known Limitations

- Дані зберігаються в пам'яті (скидаються при перезапуску)
- Один API ключ для всіх користувачів
- Немає пагінації (для великої кількості проектів)
- Немає сортування

---

## Наступні кроки / Next Steps (Optional)

1. **База даних**: TypeORM + PostgreSQL
2. **JWT Auth**: Токени замість API ключів
3. **Пагінація**: Для великих списків
4. **WebSocket**: Real-time оновлення
5. **Unit тести**: Jest тести для всіх методів
6. **Docker**: Контейнеризація

---

## Troubleshooting

### Помилка: "Module not found: @nestjs/swagger"
**Рішення**: `npm install @nestjs/swagger swagger-ui-express`

### Помилка: "Cannot find module 'class-validator'"
**Рішення**: `npm install class-validator class-transformer`

### Помилка: 401 Unauthorized
**Рішення**: Перевірити що header містить правильний API key

### Порт 3000 зайнятий
**Рішення**: Змінити PORT в .env або main.ts

---

## Контакти / Support

Якщо виникли питання:
- Перегляньте **README_PROJECTS.md** для детальної документації
- Перегляньте **SETUP.md** для інструкцій по встановленню
- Відкрийте Swagger UI для інтерактивного тестування

---

**🎉 Все готово до тестування!**

**🚀 Ready to test!**
